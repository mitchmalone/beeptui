/**
 * Application state — pure event reducer, normalized entities, selectors.
 *
 * No I/O. UI dispatches events; every state change flows through the reducer
 * (CLAUDE.md invariant 4). This is what keeps the app testable without a
 * terminal.
 *
 * Implemented in Slice 2.
 */
export {}
