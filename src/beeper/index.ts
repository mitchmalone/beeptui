/**
 * Beeper API adapter — the only module that talks to Beeper.
 *
 * Owns auth, pagination, capability detection, and error normalization. No
 * `fetch` to Beeper lives anywhere else in the codebase (CLAUDE.md invariant 3).
 *
 * Implemented in Slice 1.
 */
export {}
