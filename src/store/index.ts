/**
 * Local SQLite store (`bun:sqlite`) for non-authoritative UI state only:
 * drafts, metadata cache, last-view state, token reference. Never message
 * content or credentials (CLAUDE.md invariant 1).
 *
 * Implemented in Slice 7.
 */
export {}
