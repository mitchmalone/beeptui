/**
 * TUI layer — `@opentui/react` components, keymap, layout. Rendering and input
 * only: components render state and dispatch events, never mutating state or
 * calling the adapter directly (CLAUDE.md invariant 4).
 */
export { App } from './app.tsx'
